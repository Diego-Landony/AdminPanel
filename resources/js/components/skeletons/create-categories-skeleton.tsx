import { CreatePageSkeleton } from './CreatePageSkeleton';

export function CreateCategoriesSkeleton() {
    return (
        <CreatePageSkeleton
            sections={[
                {
                    fields: 4,
                },
            ]}
        />
    );
}
